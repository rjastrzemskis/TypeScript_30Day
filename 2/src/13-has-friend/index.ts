/**
 * Write a function that returns true if the given array includes the word "friend", false if not.
 *
 * Example
 *
 * Input: ["enemy", "friend", "enemy"]
 *
 * Output: true
 */

function hasFriend(input: string[]): boolean {
  return input.includes("friend");
}
export { hasFriend };
